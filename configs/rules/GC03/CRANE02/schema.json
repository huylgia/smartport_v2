{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "CRANE02 — config theo camera_code",
  "description": "Sinh từ internal/rules/configs.py — đừng sửa tay. Khoá là camera_code (xem configs/cranes/*.yaml); mỗi camera một dòng.",
  "type": "object",
  "additionalProperties": {
    "additionalProperties": false,
    "description": "``CRANE02`` — xe vào vị trí ổn định, và **ổn định ở đúng chỗ**.\n\nHai điều kiện, không phải một:\n\n1. bbox đầu kéo không dịch quá ``stable_move_ratio`` trong ``stable_duration``\n2. tâm bbox nằm trong ``stop_band`` tính từ **một** mép ảnh\n\nChỉ điều kiện 1 là không đủ: xe kẹt hoặc dừng chờ giữa khung hình cũng đứng yên, và nếu\nbỏ qua vị trí thì nó mở cổng OCR cho một lane không có xe nào ở đúng chỗ. Mép mà xe dừng\nlại cũng cho biết luôn chiều — xem :func:`internal.pkg.geometry.stop_side`.",
    "properties": {
      "stable_duration": {
        "default": 3.0,
        "description": "Giây. Đếm theo **thời gian**, không theo số khung: đếm khung trói định nghĩa \"ổn\nđịnh\" vào fps của nguồn, và đổi cấu hình camera là vô tình đổi luôn ngưỡng nghiệp vụ.",
        "exclusiveMinimum": 0.0,
        "title": "Stable Duration",
        "type": "number"
      },
      "stable_move_ratio": {
        "default": 0.02,
        "description": "Dịch chuyển tối đa, **tỉ lệ so với đường chéo bbox**. Ngưỡng pixel tuyệt đối phụ\nthuộc độ phân giải và khoảng cách xe tới camera. Xem DN-002 Q3.",
        "exclusiveMinimum": 0.0,
        "title": "Stable Move Ratio",
        "type": "number"
      },
      "stop_band": {
        "default": 0.35,
        "description": "Đầu xe khi dừng phải nằm trong dải này tính từ một mép ảnh, tương đối theo chiều\nrộng. Ngoài dải ⇒ **không phát signal**, dù bbox đứng yên.",
        "maximum": 1.0,
        "minimum": 0.0,
        "title": "Stop Band",
        "type": "number"
      },
      "head_thresh": {
        "default": 0.6,
        "description": "Ngưỡng tin cậy tối thiểu cho bbox đầu kéo.",
        "maximum": 1.0,
        "minimum": 0.0,
        "title": "Head Thresh",
        "type": "number"
      }
    },
    "title": "Crane02Config",
    "type": "object"
  }
}
